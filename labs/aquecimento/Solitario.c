#include <stdin.h>


int main(){
    int n;
    int A;
    int Array[100];
    scanf("%d",&n);
    while (n!=0){
        for(int i = 0;i < n; i++){
        scanf("%d",&A);
        Array[i]=A;
        }
    for(int j = 0; j < Array.length; j++){
        for(int y = Array.length-1; y => 0; y--){
        if(Array[j] == Arary[y]){
            
        }
        }
    }
    scanf("%d",&n);
    }

    return 0;
}